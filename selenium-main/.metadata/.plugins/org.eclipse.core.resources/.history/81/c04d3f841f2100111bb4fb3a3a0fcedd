package pim.module;
import java.time.Duration;
import org.openqa.selenium.support.ui.ExpectedConditions;
import org.openqa.selenium.support.ui.WebDriverWait;
import org.testng.annotations.Test;
import org.testng.asserts.SoftAssert;
import GenericUtility.BaseClass;
import POMObjectRepository.AddEmployee;
import POMObjectRepository.DashboardPage;
import POMObjectRepository.LoginPage;
import POMObjectRepository.PIMPage;

public class PimTest extends BaseClass {
  
	@Test
	public void addEmployeespim() throws InterruptedException  {
	LoginPage lp=new LoginPage(driver);
	DashboardPage dp=new DashboardPage(driver);
	PIMPage pp=new PIMPage(driver);
	AddEmployee ae=new AddEmployee(driver);
	WebDriverWait ww=new WebDriverWait(driver, Duration.ofSeconds(20));
	lp.url("https://opensource-demo.orangehrmlive.com/web/index.php/auth/login");
	lp.enterCredational("Admin", "admin123");
	lp.LoginButton();
	dp.pimlink();
	pp.addEmployee();
    ww.until(ExpectedConditions.visibilityOf(ae.getEmpId()));
    ae.createEmployee("athi", "keshan", "v", "1289");
    ww.until(ExpectedConditions.visibilityOf(ae.getUsername()));
	ae.EmployeeCredetionals("athikeshanv", "athi@2005", "athi@2005");
	ae.getSave().click();
	pp.getEmployeeList().click();
	Thread.sleep(4000);
	driver.navigate().refresh();
    boolean result = pp.listEmployeeButton("Automation");
    SoftAssert sa = new SoftAssert();
    sa.assertTrue(result, "No employee found");
    sa.assertAll();
	}
}
